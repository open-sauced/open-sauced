import styled from "styled-components";
import {size, fontSize} from "./variables";

const Container = styled.p`
  font-size: ${fontSize.default};
  margin-bottom: ${size.tiny};
  margin-right: ${size.tiny};
  outline: none;
  padding: 0;
  width: 100%;
`;

export default Container;
