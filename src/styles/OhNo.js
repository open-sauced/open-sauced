import React from "react";
import styled from "styled-components";

const OhNo = styled.svg`
  text-align: center;
  margin: auto;
  padding: 30px;

  img {
    width: 50%;
  }
`;

export default OhNo;
