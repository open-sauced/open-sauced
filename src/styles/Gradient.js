import styled from "styled-components";
import {colors} from "./variables";

export const Container = styled.div`
  background-image: linear-gradient(${colors.backgroundGrey}, ${colors.offWhite} 900px);
`;

export default Container;
